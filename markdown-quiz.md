# Markdown Skills Quiz

Complete every task in the answer area below. Do not delete the `QUIZ-START` or `QUIZ-END` markers.

## Tasks

1. Create an H1 heading with the text **My Markdown Skills**.
2. Write one sentence that contains some **bold** text and some *italic* text.
3. Create an unordered list with at least three items.
4. Create an ordered list with at least three items.
5. Add one clickable link to a useful programming or GitHub resource.
6. Show the command `git status` as inline code.
7. Create a fenced code block that contains both `git add .` and `git commit -m "markdown quiz"`.
8. Create a task list with at least two items: one checked and one unchecked.
9. Add a blockquote containing one complete sentence.
10. Create a table with at least two columns, a header row, and two data rows.

---

<!-- QUIZ-START -->
# My Markdown Skills
I am **smart**, but *lazy*.   
- 1
- 2
- 3
1. item 1
2. item 2
3. item 3      
(https://github.com/KLIS-CS/GitHub-Markdown-Skills-Quiz)     
`git status`
```
{
  git add .
  git commit -m "markdown quiz
}
```    
- [x] Homework
- [ ] read one page
> This is a quote, very good quote.

| First Header  |
| ------------- |
| Content Cell |
| Content Cell |


<!-- QUIZ-END -->
